<!DOCTYPE html>
<html>
<head>
   <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
	<title>Shoe Store</title>
</head>
<body>
<div class="container">
    <h1><a href="/">Shoe Store</a></h1>

    <h3>Brand List:</h3>
            {% if brands is not empty %}
            <h5>Click on any brand to see a list of stores where it's carried.</h5>
            <ul>
                {% for brand in brands %}
                <li><a href="/brand/ {{ brand.getId }}">{{ brand.getBrandName }}</a></li>
                {% endfor %}

            </ul>
        <br>

        {% else %}
        <p> There are no brands yet.  Add one now.</p>
            <form action="/add_brand" method= "post">
                <label for 'brand_id'>Store Name</label>
                <input id="brand_id" name="brand_id" type="text" autofocus="autofocus">

                <button type="submit">Submit</button>
            </form>
        {% endif %}
        <br>
	<button><a href="/">Home</a></button>
</div>
</body>
</html>
