<!DOCTYPE html>
<html>
<head>
   <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
	<title>Shoe Store</title>
</head>
<body>
<div class="container">
    <h1><a href="/">Shoe Store</a></h1>

    <h3>Brand List:</h3>
           <button><a href="/add_brand">Add brand</a></button>
         <br>
            {% if brands is not empty %}
            <h5>Click on any brand to see a list of brands carried.</h5>
            <ul>
                {% for brand in brands %}
                <li><a href="/brands/ {{ brand.getId }}">{{ brand.getBrandName }}</a></li>
                {% endfor %}
            </ul>
            <form action="/delete_brands" method="post">
                <button type='submit'>Delete All Brands</button>
            </form>

        <br>

        {% else %}
        <p> There are no brands yet.  Add one now.</p>
        {% endif %}


	<button><a href="/">Home</a></button>
</div>
</body>
</html>
