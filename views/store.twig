<!DOCTYPE html>
<html>
<head>
   <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
	<title>Shoe Store</title>
</head>
<body>
<div class="container">
<h3>GET: displays ONE store and all brands associated with that store</h3>

<h1><a href='/'>Shoes shoes shoes!</a></h1>

		<h3>{{ store.getStoreName }}</h3>

		{% if brands is not empty %}
			{% for brand in brands %}
				<ul>{{ brand.getBrandName }}</ul>
			{% endfor %}
		{% endif%}

		<h4>Add a brand {{ store.getStoreName }} carries</h4>
		<form action="/store/{id}" method="post">
			<label for='brand_id'>Add a brand {{ store.getStoreName }} carries</label>
			<input value="{{ store.getId }}" id="store_id" name="store_id" type="hidden">
			<select id="brand_id" name="brand_id">
				{% for brand in all_brands %}
					<option value="{{ brand.getId }}"> {{ brand.getBrandName }}</option>
				{% endfor %}
			</select>
			<br>
			<button type="submit">Submit</button>	
		</form>
<br>
	<button><a href="/stores">List of all stores</a></button>
<br>
<br>
	<button><a href="/">Home</a></button>
</div>	
</body>
</html>