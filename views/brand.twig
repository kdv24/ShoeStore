<!DOCTYPE html>
<html>
<head>
   <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
	<title>Shoe Store</title>
</head>
<body>
<div class="container">
    <h1><a href="/">Shoe Store</a></h1>
<h3>Add a new store:</h3>
	<form action="/add_store" method= "post">
		<input id="brand_id" name="store_id" type="hidden" value = "{{ brand.getId }}">
        <label for 'store_name'>Store Name</label>
        <input id = "store_id" name ='store_id' type = 'text'>
		<button type="submit">Submit</button>
	</form>
	{% for brand in brands %}
		<li><a href="/brands/ {{ brand.getId }}">{{ brand.getBrandname}}</a></li>
		{% endfor %}
	<br>

	<button><a href="/">Home</a></button>
</div>
</body>
</html>
