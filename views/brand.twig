<!DOCTYPE html>
<html>
<head>
   <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
	<title>Shoe Store</title>
</head>
<body>
<div class="container">
<h3>GET: displays ONE brand and all stores associated with that brand<br>
	POST: has option to add a new brand to DB<br>
	DELETE: has delete button</h3>

<h1><a href='/'>Shoes shoes shoes!</a></h1>

<h3>{{ brand.getBrandName }}:</h3>
	
	{% if stores is not empty %}
		{% for store in stores %}
		<ul>
			<li>{{ store.getStoreName }}</li>
		</ul>	
		{% endfor %}
	{% endif%}


	<form action="/brand/{id}" method="post">
		<label for='store_id'>Add a store where {{ brand.getBrandName }} is carried</label>
		<input value="{{ brand.getId }}" id="brand_id" name="brand_id" type="hidden">
		<select id="store_id" name="store_id">
			{% for store in all_stores %}
				<option value="{{ store.getId }}"> {{ store.getStoreName }}</option>
			{% endfor %}
		</select>
		<br>
		<button type="submit">Submit</button>
	</form>
<br>
	<button><a href="/brands">List of all brands</a></button>
<br>
<br>
	<button><a href="/">Home</a></button>
</div>	
</body>
</html>