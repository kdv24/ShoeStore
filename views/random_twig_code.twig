
<div class="container">
<h1>Shoe Store</h1>
<h3>{{ store.getStoreName }} carries the following shoes:</h3>
    {% if matching_brands is not empty %}
    <ul>
            {% for matching_brand in matching_brands %}
            <li><a href ="/brands/{{ matching_brand.getId }}"> {{ matching_brand.getBrandName }}</a>
                <form action ="/delete_brand" method="post">
                    <input name="_method" type="hidden" value="delete">
                    <input value="{{ matching_brand.getId }}" id="brand_id" name="brand_id" type="hidden">
                    <input value="{{ store.getId }}" id= "store_id" name="store_id" type="hidden">
                    <button type="submit" class="btn">Delete Brand</button>
                </form>
            </li>
            {% endfor %}
    </ul>


/////

            <form action="/store_carries" method= "post">
                <input id='store_id' name='store_id' type= "hidden" value= "{{ store.getId }}">
                <label for 'brand_id'>Brand Name</label>
                <select id="brand_id" name="brand_id" type="text">
                    {% for brand in all_brands %}
                        <option value="{{ brand.getId }}"> {{ brand.getBrandName }} </option>
                    {% endfor %}
                </select>
                <button type="submit">Submit</button>
            </form>

    <button><a href="/">Home</a></button>


////  CLICK TO SEE LIST OF BRANDS A STORE CARRIES
        {% if stores is not empty %}
    <h5>Click on any store to see a list of brands carried.</h5>
    <ul>
        {% for store in stores %}
        <li><a href="/stores/ {{ store.getId }}">{{ store.getStoreName }}</a></li>
        {% endfor %}
    </ul>
    {% endif %}


//  LIST OF STORES WHERE YOU CAN FIND A CERTAIN BRAND
   <h3>{{ brand.getBrandName }} is carried at the following stores:</h3>
    	{% if matching_stores is not empty %}
    	<ul>
			{% for matching_store in matching_stores %}
			<li><a href="/stores/ {{matching_store.getId }}"> {{ matching_store.getStoreName }}</a>
			<form action="/stores{id}/delete" method="post">
				<input name="_method" type="hidden" value="delete">
				<input value="{{ brand.getId }}" id="brand_id" name="brand_id" type="hidden">
				<input value="{{ matching_store.getId }}" id="store_id" name="store_id" type="hidden">
				<button type="submit" class="btn">Delete this store</button>
			</form>
			</li>
			{% endfor %}

// 
       <select id="brand_id" name="brand_id" type="text">
            {% for brand in all_brands %}
                <option value="{{ brand.getId }}"> {{ brand.getBrandName }} </option>
            {% endfor %}
        </select>
